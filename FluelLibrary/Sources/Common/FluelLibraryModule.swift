/// Placeholder symbol used to keep the package non-empty until shared logic moves here.
public enum FluelLibraryModule {
    /// Stable module marker for smoke tests and future integration checks.
    public static let name = "FluelLibrary"
}
